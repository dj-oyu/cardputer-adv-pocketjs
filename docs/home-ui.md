# ホームUI設計

2026-09-07試作追加: BackgroundにFLOWER RAY / FLOWER MESHを追加。
さらにLILY OF VALLEY / SUNFLOWER / SNOWDROPを追加。頂点・画像素材をFlashへ保存せず、関数から描画する。
花は背景だけとして追加し、以下の既存XMB配置と遷移を全背景で共用する。
方式・メモリ・検証範囲は[Flowerホーム試作](flower-home.md)を参照。

更新: 2026-09-06。確定済みcommit `2b053b7`の文字主体ホームを記述。チュートリアルの追加は進行中。[XMB調査](xmb-research.md)は設計経緯と初版の測定記録。

## 操作と情報構造

XMBの横カテゴリ・縦リストを文字で表現する。アイコンは使わず、名称と設定値へ表示幅を割く。
方向入力は論理的なLeft/Right/Up/Downへ変換し、Enterで決定、Esc相当のキーで戻る。
ホームは矢印刻印キー単独とFn＋矢印、Enter、左上キー単独またはFn＋同キーのEscを扱う。編集欄では文字入力と衝突しないFn付き操作を使う。
日本語入力中のEscはIMEが先に取消として処理する。通常のBackとは別にForceStop操作を設ける（[入力設計](japanese-input.md)）。

左右でApps / Settings、上下で選択カテゴリの項目を移動する。AppsはHello World、SKK Practice、Playground。SettingsはBackground、FPS Display、Sound。未実装カテゴリは並べない。
カテゴリごとの選択項目を保持し、アプリから戻ると同じ位置を選択する。
初期仕様では端で停止し、循環しない。Enterの長押しで多重起動しない。

## 240×135の配置

| 領域 | 座標の目安 | 内容 |
| --- | --- | --- |
| カテゴリ | 選択文字開始(16,37) | 文字列を96px間隔で横移動 |
| 選択項目 | 文字開始(16,69) | 2倍ASCIIフォント。明暗で選択表示 |
| 値・説明 | (16,89) | 選択項目の直下 |
| 前後の項目 | y=12 / 110付近 | 縦列が移動。カテゴリ行を横切る間は減光 |
| FPS | (194,8) | 設定で有効時のみ。常設の操作案内や機種タイトルは置かない |

選択項目を最大コントラストにし、周辺項目を暗くする。文字サイズを過度に縮小しない。
現在の同梱項目名は表示幅に収まる。任意の長い名称の省略・横スクロールは今後の設計対象。
カテゴリー切り替え時は縦リストも追従し、見た目の座標と選択状態を分けて管理する。

## 波の背景

暗い青のグラデーションと数本の波を、その場で計算して描画する。動画や全画面の大きな画像列は使わない。
波は低解像度の形状サンプルやルックアップ表で生成し、合成時にRGB565へ変換する。
半透明に見える配色はstrip内で計算し、巨大な透過レイヤーを常駐させない。
最初はネイティブShellで描画し、JSアプリと同時には動かさない。

ui_taskのフレーム目標は約30Hz。波と海面の2パターン、常時パーティクル、波のIMU連動を実装済み。現在の性能は統合commitに対して再測定する。
背景が動くと画面の広い範囲が毎回更新されるため、damage描画だけで転送が小さくなるとは仮定しない。
描画とSPI転送の合計時間を測り、負荷が高ければ背景の更新頻度・波の本数を下げる。

## 遷移と応答

現在は時定数45msの指数補間で目標位置へ追従する。120〜180msは初期設計値で、固定の遷移所要時間ではない。
入力1回ごとにアニメーション完了を待たない。時間はフレーム数ではなく単調時刻から求める。
設定はEnterで子階層を開き、上下で候補を選び、Enterで保存して戻る。Escは保存せず戻る。親を左へ移動させる。起動時の暗転演出は未実装で、アプリへ直接切り替える。
アプリ終了後は転送とメモリの解放を完了してからホームの描画を再開する。

## エラー画面

エラー種別、短い説明、戻る操作をネイティブ描画する。長いstack traceは将来のConsoleへ回す。
例外・メモリ不足からの通常復帰と、watchdog再起動を区別する。
停止確認が取れないときにホームへ遷移したように見せて、古いアプリを裏で走らせない。
